---
import BaseLayout from "@/layouts/BaseLayout.astro";
import directus from "@/lib/directus";
import { readSingleton } from "@directus/sdk";

let contactPage: any = null;
try {
  contactPage = await directus.request(readSingleton("contact_page"));
} catch {
  // Collection empty or doesn't exist — use defaults
}
---

<BaseLayout title="Start a Conversation" description="Tell us what's broken. We'll tell you if we can automate it and what it'll take.">
  <div class="container max-w-5xl mx-auto py-20 px-4 sm:px-6">
    <div class="mb-16">
      <h1 class="text-5xl font-bold tracking-tight md:text-7xl">{contactPage?.title ?? "Let's Talk"}</h1>
      <p class="mt-6 text-xl text-muted-foreground max-w-2xl">
        Whether you need a single automation or a full system, we start with a conversation.
      </p>
    </div>

    <div class="grid gap-12 md:grid-cols-2">
      <div>
        <form class="space-y-6">
          <div>
            <label for="name" class="block text-sm font-medium mb-2">Name</label>
            <input type="text" id="name" name="name" required class="w-full rounded-md border bg-background px-4 py-2.5 text-sm focus:outline-none focus:ring-2 focus:ring-ring" />
          </div>
          <div>
            <label for="email" class="block text-sm font-medium mb-2">Email</label>
            <input type="email" id="email" name="email" required class="w-full rounded-md border bg-background px-4 py-2.5 text-sm focus:outline-none focus:ring-2 focus:ring-ring" />
          </div>
          <div>
            <label for="message" class="block text-sm font-medium mb-2">What do you need help with?</label>
            <textarea id="message" name="message" rows="5" required class="w-full rounded-md border bg-background px-4 py-2.5 text-sm focus:outline-none focus:ring-2 focus:ring-ring resize-none"></textarea>
          </div>
          <button type="submit" class="inline-flex items-center justify-center rounded-md text-sm font-medium bg-primary text-primary-foreground hover:bg-primary/90 h-11 px-8">
            {contactPage?.submit_button_label ?? "Send Message"}
          </button>
        </form>
      </div>

      <div class="space-y-8">
        <div>
          <h3 class="font-semibold mb-2">Email</h3>
          <a href={`mailto:${contactPage?.email ?? "hello@arcatelabs.com"}`} class="text-muted-foreground hover:text-foreground transition-colors">{contactPage?.email ?? "hello@arcatelabs.com"}</a>
        </div>
        {contactPage?.sidebar_content ? (
          <div class="prose dark:prose-invert" set:html={contactPage.sidebar_content} />
        ) : (
          <div class="space-y-6">
            <div>
              <h3 class="font-semibold mb-2">Not sure what you need?</h3>
              <p class="text-muted-foreground">
                That's fine. Describe the problem or the process that's eating your time. We'll figure out the right approach together.
              </p>
            </div>
            <div>
              <h3 class="font-semibold mb-2">What happens next?</h3>
              <p class="text-muted-foreground">
                We'll get back to you within a business day. If there's a fit, we'll schedule a call to scope things out. No pressure, no pitch deck.
              </p>
            </div>
          </div>
        )}
      </div>
    </div>
  </div>
</BaseLayout>
